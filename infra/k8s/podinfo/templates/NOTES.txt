🌐 Podinfo deployed successfully!

Service: {{ include "podinfo.fullname" . }}
Port:    {{ .Values.service.port }}
Replicas: {{ .Values.replicaCount }}

Verify with:
  kubectl get pods -l app.kubernetes.io/name={{ include "podinfo.name" . }} -n {{ .Release.Namespace }}
  kubectl port-forward svc/{{ include "podinfo.fullname" . }} 9898:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Then open: http://localhost:9898/

Endpoints:
  GET /           — UI
  GET /healthz    — Liveness
  GET /readyz     — Readiness
  GET /version    — Version info
  GET /env        — Environment variables
