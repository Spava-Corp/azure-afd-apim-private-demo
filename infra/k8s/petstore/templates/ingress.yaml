# Ingress is not used in this architecture.
# Traffic flows: AFD → APIM (Private Endpoint) → AKS Internal LB → ClusterIP Services
# This file is a placeholder if you need path-based routing via an ingress controller.
#
# To enable nginx ingress with internal LB, uncomment and configure:
#
# {{- if false }}
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: {{ include "petstore.fullname" . }}
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /
# spec:
#   ingressClassName: nginx
#   rules:
#     - http:
#         paths:
#           - path: /petstore
#             pathType: Prefix
#             backend:
#               service:
#                 name: {{ include "petstore.fullname" . }}
#                 port:
#                   number: {{ .Values.service.port }}
# {{- end }}
